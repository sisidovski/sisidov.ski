import React from 'react'
import Particles from 'react-particles-js';

export default () => {
  <Particles 
    params={{
      particles: {
        color: {
          value: "#000000"
        }
      }
    }}
  />
}